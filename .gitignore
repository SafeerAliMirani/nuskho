node_modules
dist
dist-web
.vite
*.local
.DS_Store
.env
.env.*
# NOTE: src/ui/type.css IS committed. It carries both typefaces base64-inlined,
# which is what lets the app render identically from a folder with no network.
# src/ui/fonts/*.woff2 are the sources it was generated from and are committed
# too, so the file can be rebuilt rather than trusted.

releases/
tsconfig.tsbuildinfo

# The wire packaged as one Windows file, 55 MB, with node inside it so a clinic
# machine needs nothing installed. It is a build output, not source: rebuild it
# with `npm run wire:exe`. Binaries do not belong in git, and GitHub refuses
# them past 100 MB anyway.
tools/nuskho-wifi.exe
